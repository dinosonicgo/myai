# ==================================================
# == AI戀人專案 環境變數設定檔 (.env) v1.3
# ==================================================
#
# 更新紀錄:
# v1.3 (2025-09-01): [安全性修正] 移除了所有範例金鑰，改為空字串，使其成為一個更安全的標準範本。
# v1.2 (2050-07-31): [根本性修正] 根據 config.py v1.4 的設計，明確使用 GOOGLE_API_KEYS_1, _2 等變數名，以確保 pydantic-settings 能準確讀取。
#
# 使用說明:
# 1. 這是設定檔的範本。請將此檔案複製一份，並重新命名為 ".env"。
# 2. 程式會讀取 ".env" 檔案中的設定，而不是這個範本檔。
# 3. 請確保 ".env" 檔案與此範本檔位於同一個 "config" 資料夾內。

# --- 資料庫設定 ---
# 預設使用 SQLite，檔案將儲存在 data/ai_lover.db
# 如果您要使用其他資料庫 (如 PostgreSQL)，請修改此連接字串
DATABASE_URL="sqlite+aiosqlite:///./data/ai_lover.db"


# --- Discord Bot 設定 ---
# (必需) 您的 Discord Bot Token。請前往 Discord Developer Portal 複製您的 Token 並貼在此處。
DISCORD_BOT_TOKEN=""

# (必需) 您的 Discord 使用者 ID。用於授權您使用管理員(GM)指令。
# 如何取得：在 Discord 中，進入「設定」->「進階」，打開「開發者模式」。然後在任何地方右鍵點擊您的頭像，選擇「複製使用者 ID」。
ADMIN_USER_ID=""


# --- API 金鑰設定 ---

# (必需) Google Gemini API Keys (至少需要一個)
# [重要] 變數名必須是 'GOOGLE_API_KEYS_1', 'GOOGLE_API_KEYS_2' 等格式。
# 您可以填寫多個，程式會在一個金鑰失效時自動輪換到下一個。
GOOGLE_API_KEYS_1=""
GOOGLE_API_KEYS_2=""
GOOGLE_API_KEYS_3=""
GOOGLE_API_KEYS_4=""
GOOGLE_API_KEYS_5=""
GOOGLE_API_KEYS_6=""

# (可選，但強烈推薦) Cohere API Key
# 用於 RAG 系統中的 Rerank 模型，可以大幅提升檢索品質。
COHERE_KEY=""