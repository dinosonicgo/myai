# requirements.txt 的中文註釋(v2.4 - 消除棄用警告)
# 更新紀錄:
# v2.4 (2025-11-26): [架構優化] 新增 langchain-huggingface 依賴。根據 LangChain 官方的棄用警告，將 HuggingFaceEmbeddings 的來源遷移到這個新的獨立套件，以確保未來的兼容性並消除警告日誌。
# v2.3 (2025-11-26): [灾难性BUG修复] 鎖定 RAG 相關函式庫到一個已知的穩定組合，解決 'EncoderDecoderCache' ImportError。
# v2.2 (2025-11-26): [驗證確認] 確認現有依賴項已完整包含本地 RAG 系統所需的所有套件。

# --- 核心框架 (Core Framework) ---
langchain==0.2.11
langchain-core==0.2.36
langchain-community==0.2.10
langchain-google-genai==1.0.10
google-generativeai==0.7.1
# [v2.4 新增] 根據官方建議，用於本地 Embedding 的新套件
langchain-huggingface==0.0.3

# --- Discord 機器人 (Discord Bot) ---
discord.py==2.3.2

# --- 環境與設定 (Environment & Settings) ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.3.4

# --- 資料庫 (Database) ---
SQLAlchemy==2.0.30
aiosqlite==0.20.0

# --- 網頁伺服器 (Web Server) ---
fastapi==0.111.0
uvicorn==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
websockets==12.0

# --- RAG 系統 (Retrieval-Augmented Generation) ---
# [v2.3 核心修正] 鎖定到一個已知的穩定組合以解決 ImportError
torch==2.3.1
torchvision==0.18.1
transformers==4.36.2
sentence-transformers==2.7.0
peft==0.9.0
chromadb==0.5.3
langchain-chroma==0.1.2
rank_bm25==0.2.2

# --- 自然語言處理與工具 (NLP & Utilities) ---
spacy==3.7.5
python-Levenshtein==0.25.1
httpx==0.27.0
