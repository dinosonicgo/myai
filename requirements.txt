# requirements.txt 的中文註釋(v1.7 - 版本鎖定)
# 更新紀錄:
# v1.7 (2025-11-26):
# 1. [灾难性BUG修复] 鎖定了 torch, torchvision, transformers, sentence-transformers 等核心依賴的精確版本號。這是一組經過驗證的、能夠彼此完美兼容的「黃金版本」，旨在從根本上解決因函式庫版本衝突而導致的 `RuntimeError: operator torchvision::nms does not exist` 和 `ImportError` 等一系列致命的啟動錯誤。
# v1.6 (2025-11-26):
# 1. [灾难性BUG修复] 新增 torch>=2.6 的版本要求。

# --- 核心框架 (Core Framework) ---
langchain==0.2.5
langchain-community==0.2.5
langchain-core==0.2.9
langchain-google-genai==1.0.6
google-generativeai==0.7.1

# --- Discord 機器人 (Discord Bot) ---
discord.py==2.3.2

# --- 環境與設定 (Environment & Settings) ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.3.4

# --- 資料庫 (Database) ---
SQLAlchemy==2.0.30
aiosqlite==0.20.0

# --- 網頁伺服器 (Web Server) ---
fastapi==0.111.0
uvicorn==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
websockets==12.0

# --- RAG 系統 (Retrieval-Augmented Generation) ---
# [v1.7 核心修正] 鎖定所有 RAG 相關函式庫到一個已知的穩定組合
torch==2.3.1
torchvision==0.18.1
transformers==4.41.2
sentence-transformers==3.0.1
chromadb==0.5.3
langchain-chroma==0.1.1
rank_bm25==0.2.2

# --- 自然語言處理與工具 (NLP & Utilities) ---
spacy==3.7.5
python-Levenshtein==0.25.1
httpx==0.27.0
