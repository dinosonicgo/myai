# requirements.txt 的中文註釋(v2.6 - Google 依賴衝突修正)
# 更新紀錄:
# v2.6 (2025-10-01): [災難性BUG修復] 根據 `ResolutionImpossible` 錯誤，對 Google 相關函式庫的版本進行了精準對齊。將 `google-generativeai` 的版本從 0.7.1 微調至 0.7.2，並將 `langchain-google-genai` 保持在 1.0.7，這是一組經過驗證的、能夠解決依賴衝突的穩定組合。同時，小幅升級了 `transformers` 以保持與 langchain 生態的兼容性。
# v2.5 (2025-11-26): [灾难性BUG修复] 移除了 langchain-huggingface 依賴。
# v2.4 (2025-11-26): [架構優化] 新增 langchain-huggingface 依賴以消除棄用警告。

# --- 核心框架 (Core Framework) ---
langchain==0.2.11
langchain-core==0.2.36
langchain-community==0.2.10
# [v2.6 核心修正] 將 langchain-google-genai 和 google-generativeai 對齊到兼容版本
langchain-google-genai==1.0.7
google-generativeai==0.7.2

# --- Discord 機器人 (Discord Bot) ---
discord.py==2.3.2

# --- 環境與設定 (Environment & Settings) ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.3.4

# --- 資料庫 (Database) ---
SQLAlchemy==2.0.30
aiosqlite==0.20.0

# --- 網頁伺服器 (Web Server) ---
fastapi==0.111.0
uvicorn==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
websockets==12.0

# --- RAG 系統 (Retrieval-Augmented Generation) ---
torch==2.3.1
torchvision==0.18.1
# [v2.6 核心修正] 小幅升級 transformers 以保持與 langchain 新版本的兼容性
transformers==4.41.2
sentence-transformers==2.7.0
peft==0.9.0
chromadb==0.5.3
langchain-chroma==0.1.2
rank_bm25==0.2.2

# --- 自然語言處理與工具 (NLP & Utilities) ---
spacy==3.7.5
https://github.com/explosion/spacy-models/releases/download/zh_core_web_sm-3.7.0/zh_core_web_sm-3.7.0.tar.gz
python-Levenshtein==0.25.1
httpx==0.27.0
