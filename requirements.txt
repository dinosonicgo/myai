# requirements.txt 的中文註釋(v1.5)
# 更新紀錄:
# v1.5 (2050-08-01):
# 1. [註釋強化] 增加註釋，明確各個依賴庫的用途，並再次確認 langchain-chroma 套件已包含在內，以解決 ModuleNotFoundError。
# v1.4 (2050-07-31):
# 1. [品質提升] 新增 pydantic-settings，用於創建一個統一的、類型安全的設定管理系統，並在啟動時驗證環境變數。
# v1.3 (2050-07-31):
# 1. [RAG整合] 新增 chromadb 和 langchain-chroma。

# --- 核心框架 (Core Framework) ---
# LangChain 核心組件，用於構建 LLM 應用
langchain>=0.1.8
langchain-community>=0.0.21
langchain-core>=0.1.23
# Google Generative AI 的 LangChain 整合包
langchain-google-genai>=1.0.0
# Google AI Python SDK
google-generativeai>=0.3.2

# --- Discord 機器人 (Discord Bot) ---
# 用於與 Discord API 互動，創建機器人
discord.py>=2.3.2

# --- 環境與設定 (Environment & Settings) ---
# 用於讀取 .env 檔案中的環境變數
python-dotenv>=1.0.1
# 強大的數據驗證和設定管理庫
pydantic==2.6.1
# Pydantic 的擴展，專門用於從環境變數加載設定
pydantic-settings>=2.0.0

# --- 資料庫 (Database) ---
# SQL 工具包和 ORM
SQLAlchemy==2.0.27
# 用於 SQLAlchemy 的異步 SQLite 驅動
aiosqlite==0.19.0

# --- 網頁伺服器 (Web Server) ---
# 高性能的 Web 框架，用於本地測試介面
fastapi==0.109.2
# ASGI 伺服器，用於運行 FastAPI 應用
uvicorn==0.27.1
# 模板引擎，用於渲染 HTML 頁面
jinja2==3.1.3
# 用於處理 multipart/form-data，支援檔案上傳
python-multipart==0.0.9
# WebSocket 協議的實現，用於即時通訊
websockets==12.0

# --- 認證 (Authentication) ---
# 用於處理 JWT (JSON Web Tokens)
python-jose==3.3.0

# --- 向量儲存 (Vector Store for RAG) ---
# 用於儲存和檢索向量數據的數據庫
chromadb>=0.4.22
# ChromaDB 的 LangChain 整合包 (這是解決 ModuleNotFoundError 的關鍵)
langchain-chroma>=0.1.0