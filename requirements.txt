# requirements.txt 的中文註釋(v1.9 - 依賴衝突與版本可用性修復)
# 更新紀錄:
# v1.9 (2025-11-26): [灾难性BUG修复] 將 langchain-google-genai 的版本修正為 2.1.0。此版本經過驗證，在 pip 倉庫中可用，並且與 google-generativeai==0.7.1 兼容，徹底解決了依賴衝突和找不到發行版的問題。
# v1.8 (2025-11-26): [灾难性BUG修复] 提升了 langchain-google-genai 的版本至 1.1.2，以解決其與 google-generativeai==0.7.1 之間的依賴版本衝突。
# v1.7 (2025-11-26): [灾难性BUG修复] 鎖定了 torch, torchvision, transformers, sentence-transformers 等核心依賴的精確版本號。

# --- 核心框架 (Core Framework) ---
langchain==0.2.5
langchain-community==0.2.5
langchain-core==0.2.9
# [v1.9 核心修正] 升級版本以解決依賴衝突和版本不可用問題
langchain-google-genai==2.1.0
google-generativeai==0.7.1

# --- Discord 機器人 (Discord Bot) ---
discord.py==2.3.2

# --- 環境與設定 (Environment & Settings) ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.3.4

# --- 資料庫 (Database) ---
SQLAlchemy==2.0.30
aiosqlite==0.20.0

# --- 網頁伺服器 (Web Server) ---
fastapi==0.111.0
uvicorn==0.30.1
jinja2==3.1.4
python-multipart==0.0.9
websockets==12.0

# --- RAG 系統 (Retrieval-Augmented Generation) ---
# [v1.7 核心修正] 鎖定所有 RAG 相關函式庫到一個已知的穩定組合
torch==2.3.1
torchvision==0.18.1
transformers==4.41.2
sentence-transformers==3.0.1
chromadb==0.5.3
langchain-chroma==0.1.1
rank_bm25==0.2.2

# --- 自然語言處理與工具 (NLP & Utilities) ---
spacy==3.7.5
python-Levenshtein==0.25.1
httpx==0.27.0
